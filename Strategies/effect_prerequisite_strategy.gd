class_name EffectPrerequisiteStrategy
extends PrerequisiteStrategy

## Prerequisite type for effect activation
## Effect prerequisites are checked AFTER the card is played
## If not satisfied, the effect is skipped but the card is still played
